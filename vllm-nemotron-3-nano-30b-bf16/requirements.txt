# vLLM with CUDA support for aarch64 (DGX Spark)
# Install with: pip install -r requirements.txt

# PyTorch with CUDA 12.8 (required for aarch64 GPU support)
--extra-index-url https://download.pytorch.org/whl/cu128

torch>=2.9.0
torchvision
torchaudio

# vLLM (will use the CUDA-enabled PyTorch)
vllm>=0.12.0
